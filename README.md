# Linear Counter Demo

Small Python project for demonstrating Symphony against a real Linear project.

The initial code intentionally contains a bug in `decrement()`. The target issue for Symphony is to fix that bug and validate the change with tests.
